<section class="page-hero compact-hero">
  <div class="container">
    <span class="eyebrow">Duug Styles</span>
    <h1><%= page?.title || slug.replaceAll('-', ' ') %></h1>
    <p>Conteudo institucional da loja, preparado para orientar a compra e reforcar confianca.</p>
  </div>
</section>
<section class="section">
  <div class="container text-page">
    <h2><%= page?.title || 'Informacoes da loja' %></h2>
    <p><%= page?.body || 'Esta pagina pode ser editada pelo painel administrativo.' %></p>
  </div>
</section>
